@model ErrorViewModel
@{
    ViewData["Title"] = "錯誤";
}

<section class="page-header">
    <h1>發生錯誤</h1>
    <p class="subtitle">請稍後再試，或聯絡系統管理員。</p>
</section>

@if (Model.ShowRequestId)
{
    <p class="error-id">Request ID: <code>@Model.RequestId</code></p>
}
